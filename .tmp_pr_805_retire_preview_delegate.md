## Summary

- retire the `QfitDockWidget._current_activity_preview_request` compatibility delegate
- call the local-first activity preview request builder directly from dock-first flows
- update dock widget tests so they assert the retained dock-first behavior without relying on the removed delegate

## Why

This keeps #805 pointed at consolidating the dock-first implementation rather than preserving old UI-version compatibility layers. The visible dock-first UX is unchanged; the slice only removes one unnecessary dock wrapper around the local-first application policy.

Refs #805

## Tests

- `python3 -m unittest tests.test_qfit_dockwidget_analysis_pure tests.test_local_first_activity_controls tests.test_local_first_progress_facts -q`
- `python3 -m pytest tests/ -x -q --tb=short`
